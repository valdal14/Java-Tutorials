package com.valdal14.SocketProgramming;

/**
 *  SOCKET PROGRAMMING --------------------------------------------------------------------------------------------
 *      -> The Client sends a request to a server via HTTP but the underline protocol used is TCP.
 *      -> This is SOCKET PROGRAMMING
 *          -> The ServerSocket it will bind to a Port to listen
 *          -> The client will use a Socket to connect to the Port and IP of the server
 *          -> The Server will accept the connection
 *          -> And we can start sending and receiving messages
 *          -> Once done both connections will be closed
 *      -> java.net package
 *      -> ServerSocket and Socket
 *
 *
 *
 *  ---------------------------------------------------------------------------------------------------------------
 */

public class SocketProgrammingDemo {
}
